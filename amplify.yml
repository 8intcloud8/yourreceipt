version: 1
frontend:
  variables:
    FLUTTER_WEB: enabled
  phases:
    preBuild:
      commands:
        - git clone https://github.com/flutter/flutter.git -b stable --depth 1
        - export PATH="$PATH:`pwd`/flutter/bin" && flutter --version
        - export PATH="$PATH:`pwd`/flutter/bin" && flutter config --enable-web
        - export PATH="$PATH:`pwd`/flutter/bin" && cd frontend && flutter clean
        - export PATH="$PATH:`pwd`/flutter/bin" && cd frontend && flutter pub get
    build:
      commands:
        - export PATH="$PATH:`pwd`/flutter/bin" && cd frontend && flutter build web --release --base-href / --web-renderer html --verbose
  artifacts:
    baseDirectory: frontend/build/web
    files:
      - '**/*'
  cache:
    paths:
      - frontend/.dart_tool/**/*
      - frontend/build/**/*